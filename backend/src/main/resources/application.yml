spring:
  application:
    name: backend
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: dpn_workflow
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  secret: dpn-workflow-secret-key-2025
  expiration: 86400000
