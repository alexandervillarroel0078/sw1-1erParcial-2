name: mobile
description: Cliente móvil Workflow — políticas de negocio.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^18.0.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  go_router: ^14.6.2
  provider: ^6.1.1
  google_fonts: ^6.1.0
  stomp_dart_client: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
